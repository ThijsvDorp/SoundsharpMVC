@model IEnumerable<SoundsharpMVC.Models.VMMp3Player>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DisplayWidth)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DisplayHeight)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TotalPixels)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Picture)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MbSize)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.DisplayWidth)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.DisplayHeight)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.TotalPixels)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Picture)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.MbSize)
    </td>
    <td>
        @Html.ActionLink("Edit", "Edit", new { id = item.MbSize }) |
        @Html.ActionLink("Details", "Details", new { id = item.MbSize }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.MbSize })
    </td>
</tr>
}

</table>
